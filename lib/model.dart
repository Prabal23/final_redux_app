class NameState{
 List name;
 dynamic id = 0;
 dynamic title = '';
 dynamic body = '';
 dynamic isSelected = false;
 //dynamic title = '';
 NameState({this.id, this.title, this.body, this.name, this.isSelected});

// NameState.initialState(
//   name, title
// );
 
  
}